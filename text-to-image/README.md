# ReplaceSelectedTextAsImage.jsx

A very simple script to save the selected text as an image and replace the text with it.

With an option to navigate the cursor to the next/first content with character style in style group '~~~~~(MathTools)' (see variable searchNextMathToolsFormat).