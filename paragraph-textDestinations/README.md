# insertTextDestinations.jsx

An InDesign script that adds text destinations to the start of each paragraph. 

It skips empty paragraphs.

Paragraphs that already contain a text destination at its start will also be skipped.
